
@model Task10.Models.CookingSession


@using (@Html.BeginForm("submitCookingData", "Admin", FormMethod.Post, new { id = "formData" }))
{
    @Html.TextBoxFor(t => t.SessionId, "", new { @class = "form-control form-control-lg", @type = "hidden" })
    <div class="form-group">
        @Html.LabelFor(t => t.TranierId, "Trainer Name")
        @Html.DropDownListFor(t => t.TranierId, Model.TrainerList, "Select any Trainee", new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.TranierId, "", new { @class = "text-danger" })
    </div>
    <div class="form-group">
        @{
            // Check if the dateOfBirth is DateTime.MinValue
            //string birthdate = Model.BirthDate == DateTime.MinValue ? "" : Model.BirthDate.ToString("yyyy-MM-dd");
        }
        @Html.LabelFor(t => t.SessionDate, "Session Date")
        @Html.TextBoxFor(t => t.SessionDate, "{0:yyyy-MM-dd}", new { @class = "form-control form-control-lg", @type = "date" })
        @Html.ValidationMessageFor(m => m.SessionDate, "", new { @class = "text-danger" })

        @*<div class="col-6 mb-1">
                <label class="form-label" for="">Attendee</label>

                    @Html.DropDownListFor(x => x.AttendeeList, Model.AttendeeList, new
               {
                   @id = "attendee",
                   @class = "select2 form-select",
                   @multiple = "multiple"
               })

            </div>*@
        @*@Html.DropDownListFor(m => m.AttendeeList, new SelectList(Model.AttendeeList, "UserId", "FullName"), new { @multiple = "multiple", @placeholder = "Please select Attendee", @class = "ddlMultiSlectBox" })*@
    </div>
    <div class="form-group">
        @Html.LabelFor(t => t.AttendeeId, "Attendee Name")
        @Html.DropDownListFor(m => m.AttendeeId, Model.AttendeeList, new { @class = "form-control", multiple = "multiple" })
        @*@Html.DropDownListFor(t => t.AttendeeId, Model.AttendeeList, "Select Attendee", new { @class = "form-control" })*@
        @Html.ValidationMessageFor(m => m.AttendeeId, "", new { @class = "text-danger" })
    </div>
    <div class="mt-3 d-grid gap-2">
        <button type="submit" class="btn btn-block btn-gradient-primary btn-lg font-weight-medium auth-form-btn">Submit</button>
    </div>
}